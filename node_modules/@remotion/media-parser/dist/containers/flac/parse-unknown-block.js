export const parseFlacUnkownBlock = ({ iterator, state, size, }) => {
    iterator.discard(size);
    state.getFlacStructure().boxes.push({
        type: 'flac-header',
    });
    return Promise.resolve(null);
};
