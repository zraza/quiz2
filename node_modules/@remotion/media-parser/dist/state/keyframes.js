export const keyframesState = () => {
    const keyframes = [];
    return {
        addKeyframe: (keyframe) => {
            keyframes.push(keyframe);
        },
        getKeyframes: () => {
            return keyframes;
        },
    };
};
